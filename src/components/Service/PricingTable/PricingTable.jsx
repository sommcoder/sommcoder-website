import styled from "styled-components";

import { useState } from "react";

export default function PricingTable({
  menu,
  title,
  toggleCardState,
  cardState,
}) {
  return <StyledPricingTable></StyledPricingTable>;
}
